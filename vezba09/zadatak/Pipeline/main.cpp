#include "defines.h"

using namespace std;

int main(int argc, char* argv[])
{
    // Create the pipeline

    // Create file-reading stage and add it to the pipeline

    // Create high-pass filter stage and add it to the pipeline

    // Create clipping stage and add it to the pipeline

    // Create counting stage and add it to the pipeline

    // Run the pipeline

    // Run file-writing "stage"

    // Remove filters from pipeline before they are implicitly destroyed.

    return RET_OK;
}
